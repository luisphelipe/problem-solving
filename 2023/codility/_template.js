const { expect } = require("./_utils");

const run_tests = () => {
  expect(bruteForceSolution, 1).toEqual(1);
};

const bruteForceSolution = (input, debug = []) => {
  // 1.

  debug.push(`debug information`);

  return input;
};

run_tests();

const solution = bruteForceSolution;
